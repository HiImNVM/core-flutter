library nvm;

/// A Calculator.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;
  void xxx() {
    final String aB = 'a';
    if (aB.isEmpty) {
    }
    return;
  }
}
